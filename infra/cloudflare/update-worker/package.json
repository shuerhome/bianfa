{
  "name": "@bianfa/cf-update-worker",
  "private": true,
  "type": "module",
  "description": "bianfa 更新清单 Worker：latest.json 灰度 + KV kill switch（infra/cloudflare/update-worker）",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail --format pretty"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260905.1",
    "typescript": "7.0.2",
    "wrangler": "4.129.0"
  }
}
